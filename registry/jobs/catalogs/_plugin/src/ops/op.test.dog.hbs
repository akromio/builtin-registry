use (
  alias://expected
  op = {{opName}}
)

const (
  buildParams = op.parameterizer
  buildTitle = op.title
  handle = op.fun
)

suite(__filename, proc()
  suite("buildParams()", proc()

  end)

  suite("buildTitle()", proc()

  end)

  suite("handle()", proc()
  
  end)
end)
