export {
  plugin = "{{pluginAbbr}}"
  desc = "{{pluginDesc}}"
  tags = []
  {{#if (eq pluginType "simple")}}
  defaultOpName = "default"
  ops =
    command = use("./ops/default")
  {{else}}
  # defaultOpName = "op"
  ops =
    # command = use("./ops/op")
  {{/if}}
}
